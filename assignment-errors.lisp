;;; unassigned variable error when first sale takes places(during incrementation before entering +)
;;; when first sale takes place during incrementation when checking the type of inputs to +.
;;; FOO is not a number.